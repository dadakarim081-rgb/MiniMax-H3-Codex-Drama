# Upstream notice

The official workflow material vendored under each style skill's `references/official/` directory, and the portable `h3-prompt-writing` skill, come from [MiniMax-AI/MiniMax-H3](https://github.com/MiniMax-AI/MiniMax-H3). The exact source commit and per-skill hashes are recorded in `upstream-lock.json`.

Those upstream files are preserved as source material and remain subject to MiniMax's upstream terms. The Codex adapter entrypoints and sync utility are maintained separately in this plugin. At the pinned snapshot, the upstream Git repository does not contain a root `LICENSE` file; its README links to the MiniMax H3 Community License published with the model.
